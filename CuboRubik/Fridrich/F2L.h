#ifndef F2L_H
#define F2L_H

#include "../include/rubik.h"
#include "../include/rotaciones.h"
#include "../include/cubito.h"

// Funciones para resolver casos de F2L

// Caso 1
void resolverCasoF2L1(Rubik *rubik) {
    // Implementación del algoritmo para resolver el caso 1 de F2L
}

// Caso 2
void resolverCasoF2L2(Rubik *rubik) {
    // Implementación del algoritmo para resolver el caso 2 de F2L
}

// Caso 3
void resolverCasoF2L3(Rubik *rubik) {
    // Implementación del algoritmo para resolver el caso 3 de F2L
}

// Caso 4
void resolverCasoF2L4(Rubik *rubik) {
    // Implementación del algoritmo para resolver el caso 4 de F2L
}

// Caso 5
void resolverCasoF2L5(Rubik *rubik) {
    // Implementación del algoritmo para resolver el caso 5 de F2L
}

#endif

